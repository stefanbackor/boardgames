/**
 * English translations are not needed since roles.ts is the source of truth in English.
 * This file is kept for compatibility but should remain empty.
 */

import { RoleTranslation } from './types'

// Empty - base roles.ts already contains English text
export const roleTranslationsEn: Record<string, RoleTranslation> = {}
